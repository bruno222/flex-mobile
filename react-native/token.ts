// While in development phase (yarn start), get the token from the console and put it here.
// this will help to bypass the slow SSO process on every time, at least for one hour.
export const TOKEN_TEMP = '';
